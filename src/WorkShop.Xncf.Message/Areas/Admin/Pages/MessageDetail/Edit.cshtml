@page
@model WorkShop.Xncf.Message.Areas.Admin.Pages.MessageDetail.EditModel
@{
    ViewData["Title"] = $"{ (!string.IsNullOrEmpty(Model.Id.ToString()) ? "编辑" : "新增")}作品";
}
                            